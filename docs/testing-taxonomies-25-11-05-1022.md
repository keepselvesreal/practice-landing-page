# 테스팅 분류 체계 및 Q&A

## 📋 범주 체계

```
Testing Organization (테스트 조직화)
│
├── Test Structure (테스트 구조)
│   ├── Function-based Tests (함수 기반)
│   │   └── Use Case: Simple, isolated unit tests
│   └── Class-based Tests (클래스 기반)
│       ├── Use Case: Shared fixture, grouped behavior, parametrization
│       └── Test Suite Organization
│
├── Test Grouping Strategy (그룹화 전략)
│   ├── By Module (모듈별): test_user.py
│   ├── By Feature (기능별): test_authentication.py
│   └── By Layer (계층별): test_api.py, test_service.py
│
├── Test Class Design (테스트 클래스 설계)
│   ├── Single Responsibility (단일 책임)
│   ├── Cohesion (응집도): One class per tested unit
│   └── Granularity (세분화): 1-5 classes per file
│
└── Organization Decision Factors (조직화 결정 요소)
    ├── Architectural Style (아키텍처 스타일)
    │   ├── Layered Architecture → By Layer
    │   ├── Domain-Driven Design → By Aggregate/Bounded Context
    │   └── Microservices → By Service
    │
    ├── Team Structure (팀 구조)
    │   ├── Component Teams → By Module
    │   └── Feature Teams → By Feature
    │
    ├── Codebase Maturity (코드베이스 성숙도)
    │   ├── Early Stage → By Feature (유연성)
    │   └── Mature → By Module (안정성)
    │
    └── Test Type (테스트 유형)
        ├── Unit Tests → Mirror source structure
        ├── Integration Tests → By Feature/Scenario
        └── E2E Tests → By User Journey
```

---

## 💬 질문과 응답

### Q1
테스트 파일 명명과 테스트 파일 구성 방법이 궁금해. 가령, 클래스로 테스트 케이스를 조직화하는 경우는 어떤 경우인지. 이 경우와 테스트 케이스를 함수로 만드는 경우의 차이는 무엇인지. 클래스로 테스트 케이스 조직화할 경우 한 개 파일에 테스트 클래스 배치하는 개수는?

### A1

**클래스 기반 조직화 사용 시점:**
- 공유 픽스처(Shared Fixture) 필요: setUp/tearDown으로 공통 초기화
- 논리적 그룹화: 관련 테스트를 하나의 유닛으로 묶을 때
- 상태 공유: 테스트 간 컨텍스트 필요 시
- 파라미터화된 테스트: 여러 시나리오 체계적 테스트

**함수 기반 사용 시점:**
- 독립적인 유닛 테스트: 완전히 독립적 테스트
- 간단한 입출력 검증: 순수 함수 테스트
- 빠른 작성: 보일러플레이트 최소화

**파일당 테스트 클래스 개수:**
- 1-3개: 이상적 (높은 응집도)
- 최대 5개: 허용 가능 (관련성 높을 때)
- 5개 초과: 파일 분리 고려
- 기준: 모듈당 1 클래스, 기능당 2-3 클래스, 각 클래스는 5-15개 테스트 메서드 포함 시 적절

---

### Q2
테스트 파일을 어떤 단위로, 가령 모듈이냐 기능이냐로 조직화할지는 보통 어떻게 결정하는 거야?

### A2

**결정 기준:**

1. **프로젝트 아키텍처 따르기 (Mirror Principle)**
   - 소스 코드 구조 반영으로 테스트 위치 직관적 예측 가능
   - 예: `src/services/user_service.py` → `tests/services/test_user_service.py`

2. **테스트 유형에 따라**
   - Unit Tests: 모듈 기준 (1:1 매핑)
   - Integration Tests: 기능 기준 (여러 모듈 통합)
   - E2E Tests: 사용자 시나리오 기준

3. **변경 빈도 (Change Frequency)**
   - 자주 변하는 영역: 기능 기준 (응집도 높음)
   - 안정적 영역: 모듈 기준 (유지보수 용이)

4. **팀 협업 방식**
   - Feature Team: 기능 기준 (PR 충돌 최소화)
   - Component Team: 모듈 기준 (책임 범위 명확)

**판단 흐름:**
```
단위 테스트? → Yes → 모듈 기준
              → No ↓
여러 모듈 협업? → Yes → 기능 기준
               → No ↓
도메인 논리 복잡? → Yes → 기능 기준 (응집도 우선)
                 → No → 모듈 기준 (일관성 우선)
```

**현업 혼합 전략:**
```
tests/
├── unit/               # 모듈 기준
│   ├── test_user_service.py
│   └── test_payment_gateway.py
├── integration/        # 기능 기준
│   ├── test_checkout_flow.py
│   └── test_order_processing.py
└── e2e/               # 시나리오 기준
    └── test_user_purchase_journey.py
```
