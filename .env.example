# ============================================
# K-Beauty Landing Page - 환경변수 템플릿
# ============================================
#
# 설정 방법:
#   1. 환경에 맞게 파일 복사:
#      cp .env.example .env.dev        (로컬 개발)
#      cp .env.example .env.docker     (도커 테스트)
#      cp .env.example .env.staging    (스테이징 배포)
#      cp .env.example .env.production (프로덕션 배포)
#   2. 각 환경에 맞는 값 입력
#   3. .env.* 파일은 절대 커밋하지 말 것 (.env.example 제외)
#
# 검증:
#   실행: ./scripts/validate-env.sh .env.dev
#
# ============================================

# ==================
# 환경 설정
# ==================

# ENV
# 용도: 애플리케이션 실행 환경 구분 (.env.{ENV} 파일 선택)
# 기본값: dev (설정하지 않으면 .env.dev 자동 로드)
# Values:
#   - dev: 로컬 개발 (.env.dev 사용) - 기본값
#   - docker: CI/도커 테스트 (.env.docker 사용)
# Note:
#   - Staging/Production 배포 시에는 ENV 설정 없이 환경변수 직접 주입
#   - 로컬 개발 시 ENV를 명시하지 않으면 자동으로 .env.dev 사용
# Example: export ENV=docker (docker 환경으로 전환 시)
# ENV=

# TEST_ENV
# 용도: 테스트 동작 방식 및 데이터베이스 픽스처 선택 제어
# Required: Development, Docker
# Omit in: Staging, Production (이 환경에서는 테스트를 실행하지 않음)
# Values: local (수동/로컬 테스트), docker (CI/자동화 테스트), staging (스테이징 테스트), production (프로덕션 테스트)
# Example: TEST_ENV=local
TEST_ENV=

# BASE_URL
# 용도: 애플리케이션 기본 URL (링크, 리다이렉트, CORS에 사용)
# Required: All
# Values:
#   - Development: http://localhost:8080
#   - Docker: http://localhost:8080
#   - Staging: https://kbeauty-landing-page-staging.web.app
#   - Production: https://kbeauty-landing-page.web.app
BASE_URL=http://localhost:8080

# ==================
# 데이터베이스
# ==================

# DATABASE_URL
# 용도: PostgreSQL 데이터베이스 연결 문자열
# Required: All
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# Values:
#   - Development: postgresql://user:password@localhost:5432/kbeauty
#   - Docker: postgresql://test_user:test_pass@postgres:5432/kbeauty_test
#   - Cloud SQL: postgresql://user:pass@/dbname?host=/cloudsql/project:region:instance
# Security: 🟡 INTERNAL - 데이터베이스 인증정보 포함
# Example: postgresql://kbeauty_user:mypassword@localhost:5432/kbeauty
DATABASE_URL=

# ==================
# Google Cloud
# ==================

# GOOGLE_PLACES_API_KEY
# 용도: 위치 자동완성 및 주소 검증을 위한 Google Places API 키
# Required: All
# How to get:
#   1. Google Cloud Console > API 및 서비스 > 사용자 인증 정보
#   2. 사용자 인증 정보 만들기 > API 키
#   3. 키를 "Places API"로만 제한
#   4. 프로덕션에서는 HTTP 리퍼러 제한 추가
# Security: 🔵 PUBLIC - 프론트엔드에 노출 가능 (단, GCP Console에서 제한 설정)
# Note: API 키 제한을 설정하여 무단 사용 방지
GOOGLE_PLACES_API_KEY=

# ==================
# Firebase
# ==================

# FIREBASE_PROJECT_ID
# 용도: Firebase 프로젝트 식별자 (Frontend 배포 시 사용)
# Required: All (Frontend 배포 환경)
# Format: 프로젝트 ID 문자열
# How to get:
#   1. Firebase Console (console.firebase.google.com) > 프로젝트 설정 > 일반 > 프로젝트 ID
#   2. 또는 프로젝트 루트의 .firebaserc 파일에서 확인
# Values: kbeauty-landing-page (현재 프로젝트)
# Note: 프로젝트 생성 시 설정되며 변경 불가
FIREBASE_PROJECT_ID=kbeauty-landing-page

# ==================
# PayPal
# ==================

# PAYPAL_API_BASE
# 용도: PayPal REST API 엔드포인트 기본 URL
# Required: All
# Values:
#   - Development/Staging: https://api-m.sandbox.paypal.com (샌드박스/테스트 모드)
#   - Production: https://api-m.paypal.com (라이브 모드)
# Default: https://api-m.sandbox.paypal.com
PAYPAL_API_BASE=https://api-m.sandbox.paypal.com

# PAYPAL_CLIENT_ID
# 용도: PayPal API 인증을 위한 애플리케이션 클라이언트 ID
# Required: All
# How to get:
#   1. PayPal Developer Dashboard (developer.paypal.com) 접속
#   2. "내 앱 및 인증 정보" 메뉴로 이동
#   3. 앱 생성 또는 기존 앱 선택
#   4. 앱 세부정보에서 Client ID 복사
# Security: 🔵 PUBLIC - 프론트엔드 체크아웃에 노출 가능
# Note: 샌드박스와 프로덕션에 각각 다른 인증정보 사용
PAYPAL_CLIENT_ID=

# PAYPAL_CLIENT_SECRET
# 용도: 서버 측 API 인증을 위한 PayPal 애플리케이션 시크릿
# Required: All
# How to get: PAYPAL_CLIENT_ID와 동일한 위치 ("Secret"으로 표시됨)
# Security: 🔴 CRITICAL - 절대 프론트엔드에 노출하거나 git에 커밋하지 말 것
# Note: 샌드박스와 프로덕션에 각각 다른 인증정보 사용
PAYPAL_CLIENT_SECRET=

# PAYPAL_WEBHOOK_ID
# 용도: 결제 이벤트 검증을 위한 PayPal 웹훅 식별자
# Required: All
# How to get:
#   1. PayPal Developer Dashboard > 웹훅
#   2. 엔드포인트 URL로 웹훅 생성
#   3. 수신할 이벤트 타입 선택
#   4. 웹훅 세부정보에서 Webhook ID 복사
# Values: 웹훅 ID 문자열 (예: 6F141512VL725854W)
# Note: 샌드박스와 프로덕션에 각각 별도 웹훅 생성
PAYPAL_WEBHOOK_ID=

# ==================
# 이메일 (Gmail SMTP)
# ==================

# GMAIL_ADDRESS
# 용도: 트랜잭션 이메일 발송을 위한 Gmail 계정 주소
# Required: All
# Format: email@gmail.com
# Example: your-app@gmail.com
# Security: 🔵 PUBLIC - 이메일 주소는 수신자에게 표시됨
GMAIL_ADDRESS=

# GMAIL_APP_PASSWORD
# 용도: SMTP 인증을 위한 Gmail 앱 전용 비밀번호
# Required: All
# How to get:
#   1. Google 계정에서 2단계 인증 활성화
#   2. Google 계정 > 보안 > 2단계 인증
#   3. "앱 비밀번호" 섹션으로 스크롤
#   4. "메일" 애플리케이션용 비밀번호 생성
#   5. 16자 비밀번호 복사
# Format: 16자 코드 (공백은 선택사항)
# Security: 🔴 CRITICAL - 이메일 발송 전체 권한 부여
# Note: 일반 Gmail 비밀번호가 아님
GMAIL_APP_PASSWORD=

# ==================
# 보안
# ==================

# ENCRYPTION_KEY
# 용도: 저장된 민감 데이터 암호화를 위한 Fernet 대칭 암호화 키
# Required: All
# Format: 32바이트 base64 인코딩 (Fernet 키 형식)
# How to get: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Security: 🔴 CRITICAL - 이 키를 변경하면 모든 암호화된 데이터가 무효화됨
# Note: 각 환경마다 반드시 다른 키 사용 (dev, staging, production)
# Example: 2I8QsWMSdxbySW41U5d1hAxuoH2yG0pyK7HoFR0qMpM=
ENCRYPTION_KEY=

# ADMIN_API_KEY
# 용도: 관리자 API 엔드포인트 요청 인증을 위한 Bearer 토큰
# Required: All
# Format: URL-safe base64 문자열 (32바이트 이상 권장)
# How to get: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Security: 🔴 CRITICAL - API 전체 관리자 권한 부여
# Note: 각 환경마다 반드시 다른 키 사용 (dev, staging, production)
# Example: VjAkut2ky5hfGIrJ4TQwc_JqYc3oDBCt5MeyDC4tu74
ADMIN_API_KEY=
